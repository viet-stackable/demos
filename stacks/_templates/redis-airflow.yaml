releaseName: redis-airflow
name: redis
repo:
  name: bitnami
  url: https://charts.bitnami.com/bitnami/
version: 18.1.6
options:
  commonLabels:
    stackable.tech/vendor: Stackable
  auth:
    password: airflow
  replica:
    replicaCount: 1
